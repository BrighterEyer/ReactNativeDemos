/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 *
 * @format
 * @flow
 */

import React, {Fragment} from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  StatusBar,
} from 'react-native';


import ParcelPage from './src/ParcelPage';

const App = () => {
  return (
    <ParcelPage/>
  );
};

const styles = StyleSheet.create({
});

export default App;
